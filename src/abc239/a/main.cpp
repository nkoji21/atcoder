#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    long long h;
    cin >> h;
    
    cout << fixed << setprecision(6);
    cout << sqrt(h * (12800000 + h)) << endl;
}